#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-compare-size/bin/grunt-compare-size" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-compare-size/bin/grunt-compare-size" "$@"
  ret=$?
fi
exit $ret
